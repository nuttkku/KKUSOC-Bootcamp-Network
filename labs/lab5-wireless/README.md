# Lab 5: Wireless Network Setup

## วัตถุประสงค์
- เรียนรู้การตั้งค่า Wireless Access Point (WAP)
- เข้าใจมาตรฐาน IEEE 802.11 (Wi-Fi)
- ตั้งค่าความปลอดภัยของ Wireless Network
- เรียนรู้การเลือก Channel และ Band
- เชื่อมต่ออุปกรณ์ไร้สายเข้ากับเครือข่าย
- ทำ Wireless VLAN Segmentation

## ระยะเวลา
45-60 นาที

## ความรู้พื้นฐานที่ต้องมี
- Lab 3: Router และ Switch Configuration
- Lab 4: VLAN Configuration (เพิ่มเติม)
- ระบบเครือข่ายไร้สาย (อ่านจาก [README.md](../../README.md#ระบบเครือข่ายไร้สาย-wireless-network))

## อุปกรณ์ที่ใช้ใน Lab
- Router 2911 (1 เครื่อง)
- Switch 2960 (1 เครื่อง)
- Wireless Access Point (2 เครื่อง)
- PC (2 เครื่อง - แบบมีสาย)
- Laptop (4 เครื่อง - แบบไร้สาย)
- Smartphone (2 เครื่อง - เพิ่มเติม)

---

## ส่วนที่ 1: ทำความรู้จักกับ Wireless Standards

### มาตรฐาน IEEE 802.11

| Standard | ชื่อเรียก | ความถี่ | ความเร็วสูงสุด | ปีที่ออก |
|----------|-----------|---------|---------------|----------|
| 802.11b | Wi-Fi 1 | 2.4 GHz | 11 Mbps | 1999 |
| 802.11a | Wi-Fi 2 | 5 GHz | 54 Mbps | 1999 |
| 802.11g | Wi-Fi 3 | 2.4 GHz | 54 Mbps | 2003 |
| 802.11n | Wi-Fi 4 | 2.4/5 GHz | 600 Mbps | 2009 |
| 802.11ac | Wi-Fi 5 | 5 GHz | 3.46 Gbps | 2014 |
| 802.11ax | Wi-Fi 6 | 2.4/5/6 GHz | 9.6 Gbps | 2019 |

### ช่องสัญญาณ (Channel)

**2.4 GHz** (Channel 1-13 ในประเทศไทย):
- แนะนำใช้: Channel 1, 6, 11 (ไม่ทับซ้อนกัน)
- ข้อเสีย: มีสัญญาณรบกวนเยอะ (Bluetooth, ไมโครเวฟ)

**5 GHz** (UNII-1, UNII-2, UNII-3):
- ช่องสัญญาณเยอะกว่า (36, 40, 44, 48, 149, 153, 157, 161, 165)
- ข้อดี: สัญญาณรบกวนน้อย, ความเร็วสูงกว่า
- ข้อเสีย: ระยะทางสั้นกว่า 2.4 GHz

---

## ส่วนที่ 2: สร้าง Basic Wireless Network

### Network Topology

```
                    Internet
                       |
                   Router0
                  Gi0/0 |
                       |
                   Switch0
                 /    |    \
               PC0   AP0   AP1
                    /  \   /  \
               Laptop0 Laptop1 Laptop2 Laptop3

AP0: SSID "KKU-Office" (2.4 GHz, Channel 1)
AP1: SSID "KKU-Guest" (5 GHz, Channel 36)
```

### ขั้นตอนที่ 1: วางอุปกรณ์
1. วาง Router 2911 (1 เครื่อง)
2. วาง Switch 2960 (1 เครื่อง)
3. วาง **Access Point-PT** (2 เครื่อง)
4. วาง PC (2 เครื่อง)
5. วาง Laptop (4 เครื่อง)

**หมายเหตุ**: Laptop ใน Packet Tracer มี Wireless Module ในตัว

### ขั้นตอนที่ 2: เชื่อมต่ออุปกรณ์
- Router0 Gi0/0 → Switch0 Fa0/1
- Switch0 Fa0/2 → PC0
- Switch0 Fa0/3 → AP0 (Ethernet Port)
- Switch0 Fa0/4 → AP1 (Ethernet Port)

**Laptop**: ไม่ต้องเชื่อมสายเครือข่าย (ใช้ Wi-Fi)

---

## ส่วนที่ 3: ตั้งค่า Router และ Switch

### Router Configuration

```bash
Router>enable
Router#configure terminal
Router(config)#hostname R0

# ตั้งค่า LAN Interface
R0(config)#interface gigabitEthernet 0/0
R0(config-if)#description LAN Interface
R0(config-if)#ip address 192.168.1.1 255.255.255.0
R0(config-if)#no shutdown
R0(config-if)#exit

# ตั้งค่า DHCP Pool
R0(config)#ip dhcp pool LAN-POOL
R0(dhcp-config)#network 192.168.1.0 255.255.255.0
R0(dhcp-config)#default-router 192.168.1.1
R0(dhcp-config)#dns-server 8.8.8.8
R0(dhcp-config)#exit

# Exclude IP สำหรับ Router และอุปกรณ์ Static
R0(config)#ip dhcp excluded-address 192.168.1.1 192.168.1.10

R0(config)#exit
R0#write memory
```

### Switch Configuration (พื้นฐาน)

```bash
Switch>enable
Switch#configure terminal
Switch(config)#hostname SW0

# ตั้งค่า Management IP
SW0(config)#interface vlan 1
SW0(config-if)#ip address 192.168.1.2 255.255.255.0
SW0(config-if)#no shutdown
SW0(config-if)#exit

SW0(config)#ip default-gateway 192.168.1.1

SW0(config)#exit
SW0#write memory
```

---

## ส่วนที่ 4: ตั้งค่า Access Point แบบพื้นฐาน (Open Network)

### ขั้นตอนที่ 1: ตั้งค่า AP0
1. คลิกที่ **Access Point-PT** (AP0)
2. เลือกแท็บ **Config**
3. เลือก **Port 0** (Ethernet Interface):
   - **Static IP**: (ถ้าต้องการ Management)
   - IP Address: `192.168.1.10`
   - Subnet Mask: `255.255.255.0`
   - Gateway: `192.168.1.1`

4. เลือก **Wireless** (Port 1):
   - **SSID**: `KKU-Office`
   - **Channel**: `1` (2.4 GHz)
   - **Max Number of Users**: `20`
   - **Authentication**: `Open` (ยังไม่ใส่รหัสผ่าน)

### ขั้นตอนที่ 2: ตั้งค่า AP1
1. คลิกที่ Access Point (AP1)
2. เลือกแท็บ **Config** > **Port 0**:
   - IP Address: `192.168.1.11`
   - Subnet Mask: `255.255.255.0`
   - Gateway: `192.168.1.1`

3. เลือก **Wireless**:
   - **SSID**: `KKU-Guest`
   - **Channel**: `6` (2.4 GHz) หรือ `36` (5 GHz)
   - **Max Number of Users**: `50`
   - **Authentication**: `Open`

---

## ส่วนที่ 5: เชื่อมต่อ Laptop เข้ากับ Wi-Fi

### ขั้นตอนการเชื่อมต่อ
1. คลิกที่ **Laptop0**
2. เลือกแท็บ **Desktop** > **PC Wireless**
3. คลิก **Connect**
4. เลือก SSID **KKU-Office** จากรายการ
5. คลิก **Connect**

**ตรวจสอบ IP Address**:
- เลือก **Desktop** > **IP Configuration**
- ควรได้ IP อัตโนมัติจาก DHCP (192.168.1.x)

### ทำซ้ำสำหรับ Laptop อื่นๆ:
- **Laptop1**: เชื่อมต่อ KKU-Office
- **Laptop2**: เชื่อมต่อ KKU-Guest
- **Laptop3**: เชื่อมต่อ KKU-Guest

---

## ส่วนที่ 6: ทดสอบการเชื่อมต่อ

### ทดสอบ 1: Ping Gateway
จาก Laptop0:
```bash
ping 192.168.1.1
```
✅ ควรจะ ping ผ่าน

### ทดสอบ 2: Ping ระหว่าง Laptop
จาก Laptop0 ไป Laptop1:
```bash
ping <IP ของ Laptop1>
```
✅ ควรจะ ping ผ่าน (อยู่ใน SSID เดียวกัน)

### ทดสอบ 3: Ping ข้าม SSID
จาก Laptop0 (KKU-Office) ไป Laptop2 (KKU-Guest):
```bash
ping <IP ของ Laptop2>
```
✅ ควรจะ ping ผ่าน (อยู่ในวง LAN เดียวกัน)

### ทดสอบ 4: Ping จาก Wired ไป Wireless
จาก PC0 ไป Laptop0:
```bash
ping <IP ของ Laptop0>
```
✅ ควรจะ ping ผ่าน

---

## ส่วนที่ 7: เพิ่มความปลอดภัยด้วย WPA2-PSK

### ขั้นตอนที่ 1: ตั้งค่า WPA2 บน AP0
1. คลิกที่ AP0
2. เลือกแท็บ **Config** > **Wireless**
3. เปลี่ยนการตั้งค่า:
   - **Authentication**: `WPA2-PSK`
   - **Encryption Type**: `AES`
   - **PSK Pass Phrase**: `KKU2024@Secure`

### ขั้นตอนที่ 2: Reconnect จาก Laptop
1. คลิกที่ Laptop0
2. เลือก **Desktop** > **PC Wireless**
3. คลิก **Disconnect** (ถ้าเชื่อมต่ออยู่)
4. คลิก **Connect** แล้วเลือก **KKU-Office**
5. จะขึ้นช่องให้ใส่รหัสผ่าน
6. พิมพ์: `KKU2024@Secure`
7. คลิก **Connect**

✅ ควรเชื่อมต่อสำเร็จ

---

## ส่วนที่ 8: Wireless VLAN Segmentation (ขั้นสูง)

### Network Topology แบบมี VLAN

```
                   Router0 (Inter-VLAN Routing)
                   Gi0/0 (Trunk)
                       |
                   Switch0 (Trunk)
                 /           \
       AP0 (VLAN 10)     AP1 (VLAN 20)
         /    \              /    \
    Laptop0 Laptop1    Laptop2 Laptop3

VLAN 10: Office Network (192.168.10.0/24)
VLAN 20: Guest Network (192.168.20.0/24)
```

### ขั้นตอนที่ 1: สร้าง VLAN บน Switch

```bash
SW0#configure terminal

# สร้าง VLAN
SW0(config)#vlan 10
SW0(config-vlan)#name Office-WiFi
SW0(config-vlan)#exit

SW0(config)#vlan 20
SW0(config-vlan)#name Guest-WiFi
SW0(config-vlan)#exit

# ตั้งค่า Trunk Port ไปยัง Router
SW0(config)#interface fastEthernet 0/1
SW0(config-if)#switchport mode trunk
SW0(config-if)#exit

# ตั้งค่า Access Port สำหรับ AP0 (VLAN 10)
SW0(config)#interface fastEthernet 0/3
SW0(config-if)#description AP0-Office
SW0(config-if)#switchport mode access
SW0(config-if)#switchport access vlan 10
SW0(config-if)#exit

# ตั้งค่า Access Port สำหรับ AP1 (VLAN 20)
SW0(config)#interface fastEthernet 0/4
SW0(config-if)#description AP1-Guest
SW0(config-if)#switchport mode access
SW0(config-if)#switchport access vlan 20
SW0(config-if)#exit

SW0(config)#exit
SW0#write memory
```

### ขั้นตอนที่ 2: ตั้งค่า Router สำหรับ Inter-VLAN

```bash
R0#configure terminal

# Sub-interface สำหรับ VLAN 10
R0(config)#interface gigabitEthernet 0/0.10
R0(config-subif)#encapsulation dot1Q 10
R0(config-subif)#ip address 192.168.10.1 255.255.255.0
R0(config-subif)#exit

# Sub-interface สำหรับ VLAN 20
R0(config)#interface gigabitEthernet 0/0.20
R0(config-subif)#encapsulation dot1Q 20
R0(config-subif)#ip address 192.168.20.1 255.255.255.0
R0(config-subif)#exit

# เปิด Physical Interface
R0(config)#interface gigabitEthernet 0/0
R0(config-if)#no shutdown
R0(config-if)#exit

# ตั้งค่า DHCP Pool สำหรับ VLAN 10
R0(config)#ip dhcp pool VLAN10-POOL
R0(dhcp-config)#network 192.168.10.0 255.255.255.0
R0(dhcp-config)#default-router 192.168.10.1
R0(dhcp-config)#dns-server 8.8.8.8
R0(dhcp-config)#exit
R0(config)#ip dhcp excluded-address 192.168.10.1 192.168.10.10

# ตั้งค่า DHCP Pool สำหรับ VLAN 20
R0(config)#ip dhcp pool VLAN20-POOL
R0(dhcp-config)#network 192.168.20.0 255.255.255.0
R0(dhcp-config)#default-router 192.168.20.1
R0(dhcp-config)#dns-server 8.8.8.8
R0(dhcp-config)#exit
R0(config)#ip dhcp excluded-address 192.168.20.1 192.168.20.10

R0(config)#exit
R0#write memory
```

### ขั้นตอนที่ 3: ตั้งค่า IP ของ Access Point

**AP0** (VLAN 10):
- IP Address: `192.168.10.10`
- Subnet Mask: `255.255.255.0`
- Gateway: `192.168.10.1`

**AP1** (VLAN 20):
- IP Address: `192.168.20.10`
- Subnet Mask: `255.255.255.0`
- Gateway: `192.168.20.1`

### ทดสอบ VLAN Segmentation

**ผลลัพธ์ที่คาดหวัง**:
- Laptop0, Laptop1 (VLAN 10): ได้ IP `192.168.10.x`
- Laptop2, Laptop3 (VLAN 20): ได้ IP `192.168.20.x`
- Laptop0 สามารถ ping ไป Laptop2 ได้ (ผ่าน Router)

---

## ส่วนที่ 9: Guest Network Isolation (แยก Guest ออกจาก LAN)

### ตั้งค่า ACL บน Router เพื่อจำกัดการเข้าถึง

```bash
R0#configure terminal

# สร้าง Access List ห้าม Guest (VLAN 20) เข้าถึง Office (VLAN 10)
R0(config)#access-list 101 deny ip 192.168.20.0 0.0.0.255 192.168.10.0 0.0.0.255
R0(config)#access-list 101 permit ip any any

# ใช้ ACL กับ Sub-interface VLAN 20
R0(config)#interface gigabitEthernet 0/0.20
R0(config-subif)#ip access-group 101 in
R0(config-subif)#exit

R0(config)#exit
R0#write memory
```

### ทดสอบ Guest Isolation

**จาก Laptop2 (Guest VLAN 20)**:
```bash
# Ping ไป Gateway ของตัวเอง
ping 192.168.20.1
# ✅ ควรผ่าน

# Ping ไป Office Network
ping 192.168.10.10
# ❌ ไม่ควรผ่าน (ถูกบล็อกด้วย ACL)

# Ping ไป Internet (ถ้ามี)
ping 8.8.8.8
# ✅ ควรผ่าน
```

---

## ส่วนที่ 10: การตั้งค่า Advanced Wireless

### การเลือก Channel และ Bandwidth

**2.4 GHz**:
- Channel: 1, 6, 11 (ไม่ทับซ้อน)
- Bandwidth: 20 MHz (แนะนำ) หรือ 40 MHz

**5 GHz**:
- Channel: 36, 40, 44, 48, 149, 153, 157, 161, 165
- Bandwidth: 20/40/80 MHz

### การตั้งค่า Power Level

ใน Packet Tracer อาจมีจำกัด แต่ในอุปกรณ์จริง:
- **Power Level**: 1-20 dBm (ระดับกำลังส่ง)
- ลด Power Level ถ้าต้องการจำกัดพื้นที่ Coverage

---

## ส่วนที่ 11: Wireless Site Survey

### เครื่องมือสำหรับ Site Survey
- **Wi-Fi Analyzer** (Android/iOS)
- **Ekahau** (Professional)
- **Acrylic Wi-Fi** (Windows)

### ขั้นตอน Site Survey:
1. สำรวจสัญญาณที่มีอยู่แล้ว (Existing Networks)
2. หา Channel ที่มีสัญญาณรบกวนน้อย
3. วางตำแหน่ง AP ให้ Coverage ครอบคลุม
4. ทดสอบสัญญาณในจุดต่างๆ (Signal Strength, RSSI)
5. ปรับแต่ง Power Level และ Channel

---

## ส่วนที่ 12: คำถามท้ายบท

1. **อธิบายความแตกต่างระหว่าง 2.4 GHz และ 5 GHz**
2. **ทำไมต้องใช้ Channel 1, 6, 11 สำหรับ 2.4 GHz?**
3. **WPA2-PSK และ WPA2-Enterprise ต่างกันอย่างไร?**
4. **Guest Network Isolation คืออะไร? ทำไมจึงสำคัญ?**
5. **อธิบายการทำงานของ VLAN ในระบบ Wireless**
6. **SSID คืออะไร? ควรซ่อน SSID หรือไม่?**
7. **MAC Address Filtering ช่วยเพิ่มความปลอดภัยได้จริงหรือไม่?**
8. **Roaming คืออะไร? ทำงานอย่างไร?**

---

## ส่วนที่ 13: การส่งงาน

### สิ่งที่ต้องส่ง:
1. ไฟล์ `.pkt` ของ Lab นี้ (ตั้งชื่อว่า `Lab5-YourName.pkt`)
2. Screenshot ของ:
   - Network Topology พร้อม Wireless Coverage
   - การเชื่อมต่อ Laptop กับ Wi-Fi (PC Wireless หน้าจอ)
   - การแสดง IP Address จาก Laptop (ที่ได้จาก DHCP)
   - ผลการ ping จาก Laptop ไป Gateway
   - ผลการ ping ระหว่าง Laptop ใน VLAN ต่างกัน
   - การตั้งค่า WPA2-PSK บน Access Point
3. เอกสาร Word/PDF:
   - บันทึกขั้นตอนการทำ Lab
   - ตอบคำถามท้ายบท
   - วาด Wireless Coverage Diagram
   - อธิบายการทำงานของ Guest Network Isolation
   - แนวทางการปรับปรุงความปลอดภัยของ Wireless Network

---

## ปัญหาที่พบบ่อย (Troubleshooting)

### ปัญหา: Laptop เชื่อมต่อ Wi-Fi ไม่ได้
**วิธีแก้**:
1. ตรวจสอบว่า Laptop มี Wireless Module (ใน Physical > WPC300N)
2. ตรวจสอบว่า Access Point เปิดใช้งาน
3. ตรวจสอบ SSID และรหัสผ่าน
4. ลอง Disconnect และ Connect ใหม่

### ปัญหา: Laptop ไม่ได้ IP จาก DHCP
**วิธีแก้**:
1. ตรวจสอบว่า Router มี DHCP Pool สำหรับ VLAN นั้น
2. ตรวจสอบ VLAN ของ AP Port บน Switch
3. ตรวจสอบ IP Helper Address (ถ้าใช้ VLAN)
4. ลอง Release/Renew IP

### ปัญหา: สัญญาณ Wi-Fi อ่อน
**วิธีแก้**:
- ย้าย Access Point ใกล้กับ Laptop
- ลด Obstacles (สิ่งกีดขวาง)
- เปลี่ยน Channel
- เพิ่ม Power Level (ถ้าอุปกรณ์รองรับ)

### ปัญหา: Guest ยังเข้าถึง Office Network ได้
**วิธีแก้**:
1. ตรวจสอบ ACL บน Router
2. ตรวจสอบว่า ACL ถูกใช้กับ Interface ที่ถูกต้อง
3. ตรวจสอบ VLAN ว่าแยกถูกต้องหรือไม่

---

## สรุป

ใน Lab นี้คุณได้เรียนรู้:
- ✅ มาตรฐาน IEEE 802.11 และการเลือกใช้
- ✅ การตั้งค่า Wireless Access Point
- ✅ การเลือก Channel และ Band ที่เหมาะสม
- ✅ การเชื่อมต่ออุปกรณ์ไร้สายเข้ากับเครือข่าย
- ✅ การเพิ่มความปลอดภัยด้วย WPA2-PSK
- ✅ Wireless VLAN Segmentation
- ✅ Guest Network Isolation
- ✅ การแก้ไขปัญหาเบื้องต้น

---

## ขั้นตอนต่อไป

หลังจากเรียนจบ 5 Lab แล้ว คุณสามารถ:
1. ทำโปรเจคเชื่อมรวม Lab ทั้งหมด
2. ศึกษา Dynamic Routing (OSPF, EIGRP)
3. ศึกษา Firewall และ Security
4. ศึกษา WAN Technologies
5. เตรียมตัวสอบ CCNA

---

[⬅️ กลับไปหน้าหลัก](../../README.md) | [⬅️ Lab ก่อนหน้า](../lab4-vlan/README.md)

---

🎉 **ขอแสดงความยินดี! คุณได้เรียนจบทุก Lab แล้ว!** 🎉
