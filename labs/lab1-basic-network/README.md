# Lab 1: Basic Network และ IP Configuration

## วัตถุประสงค์
- เรียนรู้การสร้างเครือข่ายพื้นฐานใน Cisco Packet Tracer
- เข้าใจการกำหนด IP Address แบบ Static
- ทดสอบการเชื่อมต่อระหว่างเครื่องคอมพิวเตอร์ด้วยคำสั่ง ping
- เข้าใจความสำคัญของ Subnet Mask และ Gateway

## ระยะเวลา
30-45 นาที

## ความรู้พื้นฐานที่ต้องมี
- OSI Model และ TCP/IP (อ่านจาก [README.md](../../README.md#osi-model-และ-tcpip))
- IP Address และ Subnet Mask (อ่านจาก [README.md](../../README.md#ip-address-และ-network-fundamentals))

## อุปกรณ์ที่ใช้ใน Lab
- PC (2 เครื่อง)
- Switch (1 เครื่อง)
- สาย Copper Straight-Through

---

## ส่วนที่ 1: สร้าง Network Topology

### ขั้นตอนที่ 1: เปิด Cisco Packet Tracer
1. เปิดโปรแกรม Cisco Packet Tracer
2. เลือก File > New เพื่อสร้าง Project ใหม่

### ขั้นตอนที่ 2: วางอุปกรณ์
1. คลิกที่ไอคอน **Switches** ที่มุมล่างซ้าย
2. เลือก **2960 Switch** และลากวางไว้กลางหน้าจอ
3. คลิกที่ไอคอน **End Devices**
4. เลือก **PC** และลากวาง 2 เครื่องด้านซ้ายและขวาของ Switch

ผลลัพธ์ที่ได้:
```
PC0 ----------- Switch0 ----------- PC1
```

### ขั้นตอนที่ 3: เชื่อมต่ออุปกรณ์ด้วยสาย
1. คลิกที่ไอคอน **Connections** (รูปสายฟ้า)
2. เลือก **Copper Straight-Through** (เส้นสีดำทึบ)
3. คลิกที่ **PC0** > เลือก **FastEthernet0**
4. คลิกที่ **Switch0** > เลือก **FastEthernet0/1**
5. ทำซ้ำสำหรับ PC1:
   - PC1 FastEthernet0 → Switch0 FastEthernet0/2

### ขั้นตอนที่ 4: ตรวจสอบสถานะของสาย
- สังเกตสถานะจุดเชื่อมต่อ:
  - 🔴 สีแดง = กำลังเชื่อมต่อ (รอซักครู่)
  - 🟢 สีเขียว = เชื่อมต่อสำเร็จ
  - 🟠 สีส้ม = มีปัญหา

---

## ส่วนที่ 2: กำหนด IP Address แบบ Static

### ขั้นตอนที่ 1: ตั้งค่า IP สำหรับ PC0
1. คลิกที่ **PC0**
2. เลือกแท็บ **Desktop**
3. คลิก **IP Configuration**
4. เลือก **Static** (ไม่ใช่ DHCP)
5. กรอกข้อมูลดังนี้:

| ฟิลด์ | ค่าที่กำหนด |
|------|-------------|
| IP Address | `192.168.1.10` |
| Subnet Mask | `255.255.255.0` |
| Default Gateway | `192.168.1.1` |

6. ปิดหน้าต่าง

### ขั้นตอนที่ 2: ตั้งค่า IP สำหรับ PC1
1. คลิกที่ **PC1**
2. เลือกแท็บ **Desktop**
3. คลิก **IP Configuration**
4. เลือก **Static**
5. กรอกข้อมูลดังนี้:

| ฟิลด์ | ค่าที่กำหนด |
|------|-------------|
| IP Address | `192.168.1.20` |
| Subnet Mask | `255.255.255.0` |
| Default Gateway | `192.168.1.1` |

6. ปิดหน้าต่าง

---

## ส่วนที่ 3: ทดสอบการเชื่อมต่อ

### ขั้นตอนที่ 1: ใช้คำสั่ง ping
1. คลิกที่ **PC0**
2. เลือกแท็บ **Desktop**
3. คลิก **Command Prompt**
4. พิมพ์คำสั่ง:
```bash
ping 192.168.1.20
```

### ผลลัพธ์ที่ควรได้รับ:
```
Pinging 192.168.1.20 with 32 bytes of data:

Reply from 192.168.1.20: bytes=32 time<1ms TTL=128
Reply from 192.168.1.20: bytes=32 time<1ms TTL=128
Reply from 192.168.1.20: bytes=32 time<1ms TTL=128
Reply from 192.168.1.20: bytes=32 time<1ms TTL=128

Ping statistics for 192.168.1.20:
    Packets: Sent = 4, Received = 4, Lost = 0 (0% loss)
```

✅ **หากเห็น Reply from... แสดงว่าเชื่อมต่อสำเร็จ!**

❌ **หากเห็น Request timed out แสดงว่ามีปัญหา**

### ขั้นตอนที่ 2: ทดสอบจาก PC1 ไป PC0
1. คลิกที่ **PC1**
2. เปิด **Command Prompt**
3. พิมพ์:
```bash
ping 192.168.1.10
```
4. ตรวจสอบว่าได้รับ Reply กลับมาหรือไม่

---

## ส่วนที่ 4: ทดสอบ Simple PDU

### ขั้นตอนที่ 1: ใช้เครื่องมือ Simple PDU
1. คลิกที่ไอคอน **Add Simple PDU** (รูปซองจดหมาย) ที่มุมขวา
2. คลิกที่ **PC0** (เป็น Source)
3. คลิกที่ **PC1** (เป็น Destination)

### ขั้นตอนที่ 2: ดู Animation
1. คลิกปุ่ม **Play** (▶) ที่มุมล่างขวา หรือ
2. คลิก **Auto Capture/Play** เพื่อดู packet เคลื่อนที่

### สิ่งที่จะเห็น:
- Packet สีฟ้าเคลื่อนที่จาก PC0 → Switch0 → PC1
- สถานะ **Successful** (สีเขียว) แสดงว่า packet ถึงปลายทาง

---

## ส่วนที่ 5: การทดลองเพิ่มเติม

### ทดสอบ 1: Subnet ต่างกันจะเชื่อมต่อได้ไหม?
1. เปลี่ยน IP ของ PC1 เป็น `192.168.2.20` (subnet ต่างจาก PC0)
2. ลอง ping จาก PC0 ไป PC1
3. **คำถาม**: ทำไมไม่สามารถเชื่อมต่อได้?

**คำตอบ**: เพราะ PC0 อยู่ใน subnet `192.168.1.0/24` ส่วน PC1 อยู่ใน subnet `192.168.2.0/24` ซึ่งเป็นคนละวงเครือข่าย จำเป็นต้องมี Router เพื่อเชื่อมต่อระหว่าง subnet

### ทดสอบ 2: Subnet Mask ผิดจะเกิดอะไร?
1. เปลี่ยน Subnet Mask ของ PC1 เป็น `255.255.0.0`
2. ลอง ping จาก PC0 ไป PC1
3. สังเกตผลลัพธ์

---

## ส่วนที่ 6: คำถามท้ายบท

1. **Switch ทำหน้าที่อะไรในเครือข่ายนี้?**
2. **ทำไม Subnet Mask จึงสำคัญ?**
3. **Default Gateway คืออะไร? ทำไมในการทดลองนี้เราไม่ได้ใช้?**
4. **หาก PC0 มี IP เป็น 192.168.1.10 และ Subnet Mask 255.255.255.0 แล้ว PC อื่นๆ ควรมี IP เท่าไหร่ถึงจะอยู่ในวงเดียวกัน?**
5. **Layer ไหนของ OSI Model ที่ IP Address ทำงาน?**

---

## ส่วนที่ 7: การส่งงาน

### สิ่งที่ต้องส่ง:
1. ไฟล์ `.pkt` ของ Lab นี้ (ตั้งชื่อว่า `Lab1-YourName.pkt`)
2. Screenshot ของ:
   - Network Topology ที่สร้าง
   - ผลลัพธ์คำสั่ง ping ที่สำเร็จ
   - ผลการทดสอบ Simple PDU
3. เอกสาร Word/PDF ตอบคำถามท้ายบท

---

## ปัญหาที่พบบ่อย (Troubleshooting)

### ปัญหา: ping ไม่ผ่าน (Request timed out)
**วิธีแก้**:
1. ตรวจสอบว่าสายเชื่อมต่อเป็นสีเขียวหรือไม่
2. ตรวจสอบ IP Address และ Subnet Mask ให้ถูกต้อง
3. ตรวจสอบว่าใช้สาย Copper Straight-Through (ไม่ใช่ Crossover)
4. ปิด Firewall ของ PC ใน Packet Tracer (ใน Desktop > Firewall > Off)

### ปัญหา: สายเป็นสีแดงไม่เปลี่ยนเป็นเขียว
**วิธีแก้**:
1. รอสักครู่ (ประมาณ 30 วินาที)
2. ลองถอดและเสียบสายใหม่
3. ตรวจสอบว่าเลือก Port ที่ถูกต้อง (FastEthernet0 สำหรับ PC)

### ปัญหา: ไม่มีตัวเลือก IP Configuration
**วิธีแก้**:
1. ตรวจสอบว่าคลิกที่ PC จริงๆ (ไม่ใช่ Switch)
2. คลิกแท็บ **Desktop** ก่อน

---

## สรุป

ใน Lab นี้คุณได้เรียนรู้:
- ✅ การสร้าง Network Topology พื้นฐาน
- ✅ การเชื่อมต่อ PC เข้ากับ Switch
- ✅ การกำหนด IP Address แบบ Static
- ✅ การทดสอบการเชื่อมต่อด้วยคำสั่ง ping
- ✅ ความสำคัญของ Subnet Mask

**Lab ถัดไป**: [Lab 2: Cable Connection และ Device Setup](../lab2-cables-devices/README.md)

---

[⬅️ กลับไปหน้าหลัก](../../README.md)
